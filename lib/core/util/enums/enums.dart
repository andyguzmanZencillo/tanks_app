enum UpsertStatus { loading, success, error, initial }

enum DeleteStatus { loading, success, error, initial }

enum PrepareStatus { loading, success, error, initial }

enum GeneralStatus { loading, success, error, initial }
